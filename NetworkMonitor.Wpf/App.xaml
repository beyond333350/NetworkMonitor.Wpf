<Application x:Class="NetworkMonitor.Wpf.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             StartupUri="MainWindow.xaml">
    <Application.Resources>

        <SolidColorBrush x:Key="BrushBackground" Color="#F8FAFC"/>
        <SolidColorBrush x:Key="BrushControlBackground" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="BrushForeground" Color="#0F172A"/>
        <SolidColorBrush x:Key="BrushSubText" Color="#64748B"/>
        <SolidColorBrush x:Key="BrushBorder" Color="#E2E8F0"/>
        <SolidColorBrush x:Key="BrushAccent" Color="#4F46E5"/>
        <SolidColorBrush x:Key="BrushAccentHover" Color="#4338CA"/>
        <SolidColorBrush x:Key="BrushNavBackground" Color="#334155"/>
        <SolidColorBrush x:Key="BrushNavText" Color="#CBD5E1"/>
        <SolidColorBrush x:Key="BrushNavItemHover" Color="#3F4F63"/>
        <SolidColorBrush x:Key="BrushNavItemActive" Color="#475569"/>
        <SolidColorBrush x:Key="BrushSuccess" Color="#16A34A"/>
        <SolidColorBrush x:Key="BrushWarning" Color="#D97706"/>
        <SolidColorBrush x:Key="BrushDanger" Color="#DC2626"/>

        <CornerRadius x:Key="Radius">8</CornerRadius>
        <DropShadowEffect x:Key="ShadowCard" BlurRadius="15" Direction="270" RenderingBias="Quality" ShadowDepth="2" Opacity="0.08"/>
        <DropShadowEffect x:Key="ShadowButton" BlurRadius="5" Direction="270" ShadowDepth="1" Opacity="0.15"/>

        <Style TargetType="Button">
            <Setter Property="Background" Value="{StaticResource BrushAccent}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Effect" Value="{StaticResource ShadowButton}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="{StaticResource Radius}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource BrushAccentHover}"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Opacity" Value="0.8"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Background" Value="{StaticResource BrushControlBackground}"/>
            <Setter Property="Foreground" Value="{StaticResource BrushForeground}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BrushBorder}"/>
            <Setter Property="BorderThickness" Value="1.5"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border" BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                Background="{TemplateBinding Background}" 
                                CornerRadius="{StaticResource Radius}">
                            <ScrollViewer x:Name="PART_ContentHost" Focusable="false"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource BrushAccent}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="{StaticResource BrushControlBackground}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BrushBorder}"/>
            <Setter Property="BorderThickness" Value="1.5"/>
            <Setter Property="Padding" Value="10,8"/>
            <Style.Resources>
                <Style TargetType="TextBox">
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="BorderThickness" Value="0"/>
                </Style>
            </Style.Resources>
        </Style>

    </Application.Resources>
</Application>