<UserControl x:Class="NetworkMonitor.Wpf.TraceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:NetworkMonitor.Wpf"
             xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
             Background="Transparent">

    <UserControl.Resources>
        <local:LossWarningConverter x:Key="LossWarningConverter"/>
    </UserControl.Resources>

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="{DynamicResource BrushControlBackground}" CornerRadius="8" Padding="15" Margin="0,0,0,15" BorderBrush="{DynamicResource BrushBorder}" BorderThickness="1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="目标地址/域名：" VerticalAlignment="Center" Foreground="{DynamicResource BrushSubText}" FontSize="14" Margin="0,0,10,0"/>

                <Border Grid.Column="1" Background="{DynamicResource BrushBackground}" CornerRadius="6" Padding="8,6" Margin="0,0,15,0">
                    <ComboBox x:Name="CmbTarget" Text="www.baidu.com" IsEditable="True" BorderThickness="0" Background="Transparent" Foreground="{DynamicResource BrushForeground}" VerticalAlignment="Center" FontSize="15" VerticalContentAlignment="Center"/>
                </Border>

                <Button x:Name="BtnStart" Grid.Column="2" Content="▶ 开始 MTR 追踪" Width="140" Height="40" Margin="0,0,10,0" Background="#38BDF8" Foreground="White" FontWeight="Bold" Click="BtnStart_Click"/>
                <Button x:Name="BtnStop" Grid.Column="3" Content="⏹ 停止" Width="80" Height="40" Background="#EF4444" Foreground="White" FontWeight="Bold" IsEnabled="False" Click="BtnStop_Click"/>
            </Grid>
        </Border>

        <Border Grid.Row="1" Background="{DynamicResource BrushControlBackground}" CornerRadius="8" Padding="10" BorderBrush="{DynamicResource BrushBorder}" BorderThickness="1">
            <DataGrid x:Name="GridMtrNodes" 
                      AutoGenerateColumns="False" 
                      CanUserAddRows="False" 
                      CanUserSortColumns="False"
                      Background="Transparent" 
                      RowBackground="Transparent" 
                      BorderThickness="0" 
                      HeadersVisibility="Column" 
                      GridLinesVisibility="Horizontal" 
                      HorizontalGridLinesBrush="{DynamicResource BrushBorder}"
                      Foreground="{DynamicResource BrushForeground}"
                      SelectionMode="Single"
                      SelectionChanged="GridMtrNodes_SelectionChanged">

                <DataGrid.RowStyle>
                    <Style TargetType="DataGridRow">
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#0A888888"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="#1A38BDF8"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.RowStyle>

                <DataGrid.ColumnHeaderStyle>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="Foreground" Value="{DynamicResource BrushSubText}"/>
                        <Setter Property="FontWeight" Value="SemiBold"/>
                        <Setter Property="Padding" Value="10,5,10,15"/>
                        <Setter Property="BorderThickness" Value="0"/>
                    </Style>
                </DataGrid.ColumnHeaderStyle>

                <DataGrid.CellStyle>
                    <Style TargetType="DataGridCell">
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Padding" Value="10,8"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="DataGridCell">
                                    <Border Background="Transparent" Padding="{TemplateBinding Padding}">
                                        <ContentPresenter VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Foreground" Value="#38BDF8"/>
                                <Setter Property="FontWeight" Value="Bold"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.CellStyle>

                <DataGrid.Columns>
                    <DataGridTextColumn Header="跳数 (Hop)" Binding="{Binding Hop}" Width="80"/>
                    <DataGridTextColumn Header="节点 IP (Host)" Binding="{Binding IpAddress}" Width="250" FontWeight="SemiBold"/>

                    <DataGridTemplateColumn Header="丢包率 (Loss %)" Width="120">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock x:Name="TxtLoss" Text="{Binding LossPercentStr}" FontWeight="Bold" Foreground="#4ADE80"/>
                                <DataTemplate.Triggers>
                                    <DataTrigger Binding="{Binding LossValue, Converter={StaticResource LossWarningConverter}}" Value="True">
                                        <Setter TargetName="TxtLoss" Property="Foreground" Value="#EF4444"/>
                                    </DataTrigger>
                                </DataTemplate.Triggers>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTextColumn Header="已发 (Snt)" Binding="{Binding Sent}" Width="80"/>
                    <DataGridTextColumn Header="当前 (Last)" Binding="{Binding LastPingStr}" Width="90"/>
                    <DataGridTextColumn Header="平均 (Avg)" Binding="{Binding AvgPingStr}" Width="90"/>
                    <DataGridTextColumn Header="最快 (Best)" Binding="{Binding BestPingStr}" Width="90"/>
                    <DataGridTextColumn Header="最慢 (Wrst)" Binding="{Binding WorstPingStr}" Width="90"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <Border Grid.Row="2" Height="1" Background="{DynamicResource BrushBorder}" Margin="0,15"/>

        <Border Grid.Row="3" Background="{DynamicResource BrushControlBackground}" CornerRadius="8" Padding="15" BorderBrush="{DynamicResource BrushBorder}" BorderThickness="1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock x:Name="TxtChartTitle" Grid.Row="0" Text="📊 节点延迟波动图 (请在上方表格中点击选择一个节点)" FontSize="14" FontWeight="SemiBold" Foreground="{DynamicResource BrushForeground}" Margin="0,0,0,10"/>

                <lvc:CartesianChart Grid.Row="1" DisableAnimations="True" Hoverable="False" DataTooltip="{x:Null}">
                    <lvc:CartesianChart.Series>
                        <lvc:LineSeries Values="{Binding SelectedNodeHistory}" 
                                        Title="延迟 (ms)" 
                                        Stroke="#F59E0B" 
                                        StrokeThickness="2" 
                                        PointGeometrySize="8"
                                        DataLabels="True">
                            <lvc:LineSeries.Fill>
                                <SolidColorBrush Color="#F59E0B" Opacity="0.1"/>
                            </lvc:LineSeries.Fill>
                        </lvc:LineSeries>
                    </lvc:CartesianChart.Series>
                    <lvc:CartesianChart.AxisX>
                        <lvc:Axis ShowLabels="False" Foreground="{DynamicResource BrushSubText}"/>
                    </lvc:CartesianChart.AxisX>
                    <lvc:CartesianChart.AxisY>
                        <lvc:Axis Title="延迟 (ms)" MinValue="0" Foreground="{DynamicResource BrushSubText}"/>
                    </lvc:CartesianChart.AxisY>
                </lvc:CartesianChart>
            </Grid>
        </Border>
    </Grid>
</UserControl>